# Autopilot v0.1.0

Initial release of Autopilot.

## Downloads

| Platform | File |
|----------|------|
| Windows x64 | `autopilot-win32-x64.zip` (344 MB) |
| macOS ARM64 | `autopilot-darwin-arm64.zip` (310 MB) |
| macOS x64 | `autopilot-darwin-x64.zip` (319 MB) |
| Linux x64 | `autopilot-linux-x64.tar.gz` (311 MB) |

All builds are unsigned portable archives — no installer required.
Windows SmartScreen may flag the unsigned executable.

To use: extract the archive and run the application.

> **Note:** These files are stored in the repo for easy access. Cloning the repo will include these artifacts, making the download larger than a source-only clone.
